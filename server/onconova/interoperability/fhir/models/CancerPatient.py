# Code autogenerated by Fhircraft 0.5.0 on 2026-04-11 19:03:14.583458.

from fhircraft.fhir.resources.datatypes.R4.core.patient import Patient, PatientCommunication, PatientContact
from typing import Annotated, ClassVar, List, Optional, Union
from fhircraft.fhir.resources.datatypes.R4.complex import Address, Age, Annotation, Attachment, CodeableConcept, Coding, ContactDetail, ContactPoint, Contributor, Count, DataRequirement, Distance, Dosage, Duration, Expression, Extension, HumanName, Identifier, Meta, Money, ParameterDefinition, Period, Quantity, Range, Ratio, Reference, RelatedArtifact, SampledData, Signature, Timing, TriggerDefinition, UsageContext
from fhircraft.fhir.resources.datatypes.R4.primitive import Base64Binary, Boolean, Canonical, Code, Date, DateTime, Decimal, Id, Instant, Integer, Markdown, Oid, PositiveInt, String, Time, UnsignedInt, Uri, Url, Uuid
from pydantic import Field, field_validator, model_validator
from fhircraft.fhir.resources.validators import get_type_choice_value_by_base, validate_FHIR_element_fixed_value, validate_FHIR_element_pattern, validate_element_constraint, validate_model_constraint, validate_slicing_cardinalities, validate_type_choice_element
from fhircraft.fhir.resources.base import FHIRSliceModel, FhirBaseModelKind
NoneType = type(None)

 
class USCorePatientProfileIdentifier(Identifier):
    """
    An identifier for this patient.
    """


    system: Optional[Uri] = Field(
        description="Establishes the namespace for the value - that is, a URL that describes a set values that are unique.",
        default=None,
    )
    value: Optional[String] = Field(
        description="The portion of the identifier typically relevant to the user and which is unique within the context of the system.",
        default=None,
    )
    
 
class USCorePatientProfileName(HumanName):
    """
    A name associated with the individual.
    """


    use: Optional[Code] = Field(
        description="Identifies the purpose for this name.",
        default=None,
    )
    family: Optional[String] = Field(
        description="The part of a name that links to the genealogy. In some cultures (e.g. Eritrea) the family name of a son is the first name of his father.",
        default=None,
    )
    given: Optional[List[String]] = Field(
        description="Given name.",
        default=None,
    )
    suffix: Optional[List[String]] = Field(
        description="Part of the name that is acquired as a title due to academic, legal, employment or nobility status, etc. and that appears at the end of the name.",
        default=None,
    )
    period: Optional[Period] = Field(
        description="Indicates the period of time when this name was valid for the named person.",
        default=None,
    )
    
    @model_validator(mode="after")
    def FHIR_us_core_6_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="(family.exists() or given.exists()) xor extension.where(url='http://hl7.org/fhir/StructureDefinition/data-absent-reason').exists()",
            human='At least name.given and/or name.family are present or, if neither is available, the Data Absent Reason Extension is present.',
            key='us-core-6',
            severity='error',
        )
        
 
class USCorePatientProfileTelecom(ContactPoint):
    """
    A contact detail (e.g. a telephone number or an email address) by which the individual may be contacted.
    """


    system: Optional[Code] = Field(
        description="Telecommunications form for contact point - what communications system is required to make use of the contact.",
        default=None,
    )
    value: Optional[String] = Field(
        description="The actual contact point details, in a form that is meaningful to the designated communication system (i.e. phone number or email address).",
        default=None,
    )
    use: Optional[Code] = Field(
        description="Identifies the purpose for the contact point.",
        default=None,
    )
    
 
class USCorePatientProfileAddress(Address):
    """
    An address for the individual.
    """


    use: Optional[Code] = Field(
        description="The purpose of this address.",
        default=None,
    )
    line: Optional[List[String]] = Field(
        description="This component contains the house number, apartment number, street name, street direction,  P.O. Box number, delivery hints, and similar address information.",
        default=None,
    )
    city: Optional[String] = Field(
        description="The name of the city, town, suburb, village or other community or delivery center.",
        default=None,
    )
    state: Optional[String] = Field(
        description="Sub-unit of a country with limited sovereignty in a federally organized country. A code may be used if codes are in common use (e.g. US 2 letter state codes).",
        default=None,
    )
    postalCode: Optional[String] = Field(
        description="A postal code designating a region defined by the postal service.",
        default=None,
    )
    period: Optional[Period] = Field(
        description="Time period when address was/is in use.",
        default=None,
    )
    
 
class USCorePatientProfileCommunication(PatientCommunication):
    """
    A language which may be used to communicate with the patient about his or her health.
    """


    language: Optional[CodeableConcept] = Field(
        description="The ISO-639-1 alpha 2 code in lower case for the language, optionally followed by a hyphen and the ISO-3166-1 alpha 2 code for the region in upper case; e.g. \"en\" for English, or \"en-US\" for American English versus \"en-EN\" for England English.",
        default=None,
    )
    
 
class USCorePatientProfile(Patient):
    """
    Information about an individual or animal receiving health care services
    """

    _abstract = True
    _canonical_url = "http://hl7.org/fhir/us/core/StructureDefinition/us-core-patient"

    extension: Optional[List[Extension]] = Field(
        description="May be used to represent additional information that is not part of the basic definition of the resource. To make the use of extensions safe and manageable, there is a strict set of governance  applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension.",
        default=None,
    )
    identifier: Optional[List[USCorePatientProfileIdentifier]] = Field(
        description="An identifier for this patient.",
        default=None,
    )
    name: Optional[List[USCorePatientProfileName]] = Field(
        description="A name associated with the individual.",
        default=None,
    )
    telecom: Optional[List[USCorePatientProfileTelecom]] = Field(
        description="A contact detail (e.g. a telephone number or an email address) by which the individual may be contacted.",
        default=None,
    )
    gender: Optional[Code] = Field(
        description="Administrative Gender - the gender that the patient is considered to have for administration and record keeping purposes.",
        default=None,
    )
    birthDate: Optional[Date] = Field(
        description="The date of birth for the individual.",
        default=None,
    )
    deceasedBoolean: Optional[Boolean] = Field(
        description="Indicates if the individual is deceased or not.",
        default=None,
    )
    deceasedDateTime: Optional[DateTime] = Field(
        description="Indicates if the individual is deceased or not.",
        default=None,
    )
    address: Optional[List[USCorePatientProfileAddress]] = Field(
        description="An address for the individual.",
        default=None,
    )
    communication: Optional[List[USCorePatientProfileCommunication]] = Field(
        description="A language which may be used to communicate with the patient about his or her health.",
        default=None,
    )
    
    @property 
    def deceased(self):
        return get_type_choice_value_by_base(self, 
            base="deceased",
        )
        
    @model_validator(mode="after")
    def FHIR_dom_2_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.contained.empty()',
            human='If the resource is contained in another resource, it SHALL NOT contain nested Resources',
            key='dom-2',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_3_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().as(canonical) | %resource.descendants().as(uri) | %resource.descendants().as(url))) or descendants().where(reference = '#').exists() or descendants().where(as(canonical) = '#').exists() or descendants().where(as(canonical) = '#').exists()).not()).trace('unmatched', id).empty()",
            human='If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource',
            key='dom-3',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_4_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()',
            human='If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated',
            key='dom-4',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_5_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.meta.security.empty()',
            human='If a resource is contained in another resource, it SHALL NOT have a security label',
            key='dom-5',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_6_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='text.`div`.exists()',
            human='A resource should have narrative for robust management',
            key='dom-6',
            severity='warning',
        )
        
    @model_validator(mode="after")
    def deceased_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Boolean', 'DateTime'],
            field_name_base="deceased",
            required=False,
        )
        
    @model_validator(mode="after")
    def FHIR_us_core_6_constraint_validator(self):
        return validate_element_constraint( 
            self,
            elements="name",
            expression="(family.exists() or given.exists()) xor extension.where(url='http://hl7.org/fhir/StructureDefinition/data-absent-reason').exists()",
            human='At least name.given and/or name.family are present or, if neither is available, the Data Absent Reason Extension is present.',
            key='us-core-6',
            severity='error',
        )
        
 
class CancerPatient(USCorePatientProfile):
    """
    Information about an individual or animal receiving health care services
    """

    _canonical_url = "http://hl7.org/fhir/us/mcode/StructureDefinition/mcode-cancer-patient"

    pass
    
    @property 
    def deceased(self):
        return get_type_choice_value_by_base(self, 
            base="deceased",
        )
        
    @model_validator(mode="after")
    def FHIR_dom_2_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.contained.empty()',
            human='If the resource is contained in another resource, it SHALL NOT contain nested Resources',
            key='dom-2',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_3_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="contained.where((('#'+id in (%resource.descendants().reference | %resource.descendants().as(canonical) | %resource.descendants().as(uri) | %resource.descendants().as(url))) or descendants().where(reference = '#').exists() or descendants().where(as(canonical) = '#').exists() or descendants().where(as(canonical) = '#').exists()).not()).trace('unmatched', id).empty()",
            human='If the resource is contained in another resource, it SHALL be referred to from elsewhere in the resource or SHALL refer to the containing resource',
            key='dom-3',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_4_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.meta.versionId.empty() and contained.meta.lastUpdated.empty()',
            human='If a resource is contained in another resource, it SHALL NOT have a meta.versionId or a meta.lastUpdated',
            key='dom-4',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_5_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='contained.meta.security.empty()',
            human='If a resource is contained in another resource, it SHALL NOT have a security label',
            key='dom-5',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_dom_6_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='text.`div`.exists()',
            human='A resource should have narrative for robust management',
            key='dom-6',
            severity='warning',
        )
        
    @model_validator(mode="after")
    def deceased_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Boolean', 'DateTime'],
            field_name_base="deceased",
            required=False,
        )
        
 
class OnconovaCancerPatientOnconovaIdentifier(USCorePatientProfileIdentifier, FHIRSliceModel):
    """
    An identifier for this patient.
    """
    min_cardinality: ClassVar[int] = 1
    max_cardinality: ClassVar[int | None] = None


    type: Optional[CodeableConcept] = Field(
        description="A coded type for the identifier that can be used to determine which identifier to use for a specific purpose.",
        default_factory=lambda: CodeableConcept(coding=[Coding(code='ACSN', display='Accession Identifier', system='http://terminology.hl7.org/CodeSystem/v2-0203')]),
    )
    system: Optional[Uri] = Field(
        description="Establishes the namespace for the value - that is, a URL that describes a set values that are unique.",
        default_factory=lambda: 'Onconova',
    )
    
    @field_validator(*('type',), mode="after", check_fields=None)
    @classmethod
    def FHIR_type_pattern_constraint(cls, value):    
        return validate_FHIR_element_pattern(cls, value, 
            pattern=CodeableConcept(coding=[Coding(code='ACSN', display='Accession Identifier', system='http://terminology.hl7.org/CodeSystem/v2-0203')]),
        )
        
    @field_validator(*('system',), mode="after", check_fields=None)
    @classmethod
    def FHIR_system_pattern_constraint(cls, value):    
        return validate_FHIR_element_pattern(cls, value, 
            pattern='Onconova',
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
 
class OnconovaCancerPatientClinicalIdentifier(USCorePatientProfileIdentifier, FHIRSliceModel):
    """
    An identifier for this patient.
    """
    min_cardinality: ClassVar[int] = 1
    max_cardinality: ClassVar[int | None] = None


    type: Optional[CodeableConcept] = Field(
        description="A coded type for the identifier that can be used to determine which identifier to use for a specific purpose.",
        default_factory=lambda: CodeableConcept(coding=[Coding(code='MR', display='Medical Record Number', system='http://terminology.hl7.org/CodeSystem/v2-0203')]),
    )
    
    @field_validator(*('type',), mode="after", check_fields=None)
    @classmethod
    def FHIR_type_pattern_constraint(cls, value):    
        return validate_FHIR_element_pattern(cls, value, 
            pattern=CodeableConcept(coding=[Coding(code='MR', display='Medical Record Number', system='http://terminology.hl7.org/CodeSystem/v2-0203')]),
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
 
class DataAbsentReason(Extension):
    """
    Provides a reason why the expected value or elements in the element that is extended are missing.
    """

    _abstract = True
    _canonical_url = "http://hl7.org/fhir/StructureDefinition/data-absent-reason"

    extension: None = Field(
        description="Additional content defined by implementations",
        default=None,
    )
    url: Optional[Uri] = Field(
        description="Source of the definition for the extension code - a logical name or a URL.",
        default_factory=lambda: 'http://hl7.org/fhir/StructureDefinition/data-absent-reason',
    )
    valueCode: Optional[Code] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default=None,
    )
    
    @property 
    def value(self):
        return get_type_choice_value_by_base(self, 
            base="value",
        )
        
    @field_validator(*('extension',), mode="after", check_fields=None)
    @classmethod
    def extension_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="extension",
        )
        
    @field_validator(*('url',), mode="after", check_fields=None)
    @classmethod
    def FHIR_url_fixed_value_constraint(cls, value):    
        return validate_FHIR_element_fixed_value(cls, value, 
            constant='http://hl7.org/fhir/StructureDefinition/data-absent-reason',
        )
        
    @model_validator(mode="after")
    def value_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Code'],
            field_name_base="value",
            required=True,
        )
        
    @model_validator(mode="after")
    def FHIR_ext_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='extension.exists() != value.exists()',
            human='Must have either extensions or value[x], not both',
            key='ext-1',
            severity='error',
        )
        
 
class OnconovaCancerPatientAnonymizedEntry(DataAbsentReason, FHIRSliceModel):
    """
    May be used to represent additional information that is not part of the basic definition of the element. To make the use of extensions safe and manageable, there is a strict set of governance  applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension.
    """
    min_cardinality: ClassVar[int] = 1
    max_cardinality: ClassVar[int | None] = None


    valueBase64Binary: Optional[Base64Binary] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueBoolean: Optional[Boolean] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueCanonical: Optional[Canonical] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueCode: Optional[Code] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDate: Optional[Date] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDateTime: Optional[DateTime] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDecimal: Optional[Decimal] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueId: Optional[Id] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueInstant: Optional[Instant] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueInteger: Optional[Integer] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueMarkdown: Optional[Markdown] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueOid: Optional[Oid] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valuePositiveInt: Optional[PositiveInt] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueString: Optional[String] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueTime: Optional[Time] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueUnsignedInt: Optional[UnsignedInt] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueUri: Optional[Uri] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueUrl: Optional[Url] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueUuid: Optional[Uuid] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueAddress: Optional[Address] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueAge: Optional[Age] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueAnnotation: Optional[Annotation] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueAttachment: Optional[Attachment] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueCodeableConcept: Optional[CodeableConcept] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueCoding: Optional[Coding] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueContactPoint: Optional[ContactPoint] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueCount: Optional[Count] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDistance: Optional[Distance] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDuration: Optional[Duration] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueHumanName: Optional[HumanName] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueIdentifier: Optional[Identifier] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueMoney: Optional[Money] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valuePeriod: Optional[Period] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueQuantity: Optional[Quantity] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueRange: Optional[Range] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueRatio: Optional[Ratio] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueReference: Optional[Reference] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueSampledData: Optional[SampledData] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueSignature: Optional[Signature] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueTiming: Optional[Timing] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueContactDetail: Optional[ContactDetail] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueContributor: Optional[Contributor] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDataRequirement: Optional[DataRequirement] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueExpression: Optional[Expression] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueParameterDefinition: Optional[ParameterDefinition] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueRelatedArtifact: Optional[RelatedArtifact] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueTriggerDefinition: Optional[TriggerDefinition] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueUsageContext: Optional[UsageContext] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueDosage: Optional[Dosage] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    valueMeta: Optional[Meta] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default_factory=lambda: 'masked',
    )
    
    @property 
    def value(self):
        return get_type_choice_value_by_base(self, 
            base="value",
        )
        
    @field_validator(*('valueMeta',), mode="after", check_fields=None)
    @classmethod
    def FHIR_valueMeta_pattern_constraint(cls, value):    
        return validate_FHIR_element_pattern(cls, value, 
            pattern='masked',
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
    @model_validator(mode="after")
    def value_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Base64Binary', 'Boolean', 'Canonical', 'Code', 'Date', 'DateTime', 'Decimal', 'Id', 'Instant', 'Integer', 'Markdown', 'Oid', 'PositiveInt', 'String', 'Time', 'UnsignedInt', 'Uri', 'Url', 'Uuid', 'Address', 'Age', 'Annotation', 'Attachment', 'CodeableConcept', 'Coding', 'ContactPoint', 'Count', 'Distance', 'Duration', 'HumanName', 'Identifier', 'Money', 'Period', 'Quantity', 'Range', 'Ratio', 'Reference', 'SampledData', 'Signature', 'Timing', 'ContactDetail', 'Contributor', 'DataRequirement', 'Expression', 'ParameterDefinition', 'RelatedArtifact', 'TriggerDefinition', 'UsageContext', 'Dosage', 'Meta'],
            field_name_base="value",
            required=False,
        )
        
    @model_validator(mode="after")
    def FHIR_ext_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='extension.exists() != value.exists()',
            human='Must have either extensions or value[x], not both',
            key='ext-1',
            severity='error',
        )
        
 
class OnconovaCancerPatientName(USCorePatientProfileName):
    """
    Not used in this profile
    """


    extension: Optional[list[Union[OnconovaCancerPatientAnonymizedEntry, Extension]]] = Field(
        description="Additional content defined by implementations",
        default=None,
    )
    
    @field_validator(*('extension',), mode="after", check_fields=None)
    @classmethod
    def extension_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="extension",
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_us_core_6_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="(family.exists() or given.exists()) xor extension.where(url='http://hl7.org/fhir/StructureDefinition/data-absent-reason').exists()",
            human='At least name.given and/or name.family are present or, if neither is available, the Data Absent Reason Extension is present.',
            key='us-core-6',
            severity='error',
        )
        
 
class CancerPatientAge(Extension):
    """
    The approximate age of the patient.
    """

    _abstract = True
    _canonical_url = "http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age"

    extension: None = Field(
        description="Additional content defined by implementations",
        default=None,
    )
    url: Optional[Uri] = Field(
        description="Source of the definition for the extension code - a logical name or a URL.",
        default_factory=lambda: 'http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age',
    )
    valueInteger: Optional[Integer] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default=None,
    )
    
    @property 
    def value(self):
        return get_type_choice_value_by_base(self, 
            base="value",
        )
        
    @field_validator(*('extension',), mode="after", check_fields=None)
    @classmethod
    def extension_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="extension",
        )
        
    @field_validator(*('url',), mode="after", check_fields=None)
    @classmethod
    def FHIR_url_fixed_value_constraint(cls, value):    
        return validate_FHIR_element_fixed_value(cls, value, 
            constant='http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age',
        )
        
    @model_validator(mode="after")
    def value_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Integer'],
            field_name_base="value",
            required=False,
        )
        
    @model_validator(mode="after")
    def FHIR_ext_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='extension.exists() != value.exists()',
            human='Must have either extensions or value[x], not both',
            key='ext-1',
            severity='error',
        )
        
 
class OnconovaCancerPatientAge(CancerPatientAge, FHIRSliceModel):
    """
    May be used to represent additional information that is not part of the basic definition of the resource. To make the use of extensions safe and manageable, there is a strict set of governance  applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension.
    """
    min_cardinality: ClassVar[int] = 0
    max_cardinality: ClassVar[int | None] = 1


    pass
    
 
class CancerPatientAgeAtDiagnosis(Extension):
    """
    The approximate age of the patient at the time of diagnosis of the disease.
    """

    _abstract = True
    _canonical_url = "http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age-at-diagnosis"

    extension: None = Field(
        description="Additional content defined by implementations",
        default=None,
    )
    url: Optional[Uri] = Field(
        description="Source of the definition for the extension code - a logical name or a URL.",
        default_factory=lambda: 'http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age-at-diagnosis',
    )
    valueInteger: Optional[Integer] = Field(
        description="Value of extension - must be one of a constrained set of the data types (see [Extensibility](extensibility.html) for a list).",
        default=None,
    )
    
    @property 
    def value(self):
        return get_type_choice_value_by_base(self, 
            base="value",
        )
        
    @field_validator(*('extension',), mode="after", check_fields=None)
    @classmethod
    def extension_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="extension",
        )
        
    @field_validator(*('url',), mode="after", check_fields=None)
    @classmethod
    def FHIR_url_fixed_value_constraint(cls, value):    
        return validate_FHIR_element_fixed_value(cls, value, 
            constant='http://onconova.github.io/fhir/StructureDefinition/onconova-ext-cancer-patient-age-at-diagnosis',
        )
        
    @model_validator(mode="after")
    def value_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['Integer'],
            field_name_base="value",
            required=False,
        )
        
    @model_validator(mode="after")
    def FHIR_ext_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='extension.exists() != value.exists()',
            human='Must have either extensions or value[x], not both',
            key='ext-1',
            severity='error',
        )
        
 
class OnconovaCancerPatientAgeAtDiagnosis(CancerPatientAgeAtDiagnosis, FHIRSliceModel):
    """
    May be used to represent additional information that is not part of the basic definition of the resource. To make the use of extensions safe and manageable, there is a strict set of governance  applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension.
    """
    min_cardinality: ClassVar[int] = 0
    max_cardinality: ClassVar[int | None] = 1


    pass
    
 
class OnconovaCancerPatientBirthDate(Date):
    """
    The date of birth for the individual.
    """


    extension: Optional[list[Union[OnconovaCancerPatientAge, OnconovaCancerPatientAgeAtDiagnosis, Extension]]] = Field(
        description="Additional content defined by implementations",
        default=None,
    )
    
    @field_validator(*('extension',), mode="after", check_fields=None)
    @classmethod
    def extension_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="extension",
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
 
class OnconovaCancerPatient(CancerPatient):
    """
    Information about an individual or animal receiving health care services
    """

    _canonical_url = "http://onconova.github.io/fhir/StructureDefinition/onconova-cancer-patient"

    extension: Optional[List[Extension]] = Field(
        description="May be used to represent additional information that is not part of the basic definition of the resource. To make the use of extensions safe and manageable, there is a strict set of governance  applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension.",
        default=None,
    )
    identifier: Optional[list[Union[OnconovaCancerPatientOnconovaIdentifier, OnconovaCancerPatientClinicalIdentifier, USCorePatientProfileIdentifier]]] = Field(
        description="(USCDI) An identifier for this patient",
        default=None,
    )
    name: Optional[List[OnconovaCancerPatientName]] = Field(
        description="Not used in this profile",
        default=None,
    )
    telecom: Optional[List[ContactPoint]] = Field(
        description="Not used in this profile",
        default=None,
    )
    birthDate: Optional[OnconovaCancerPatientBirthDate] = Field(
        description="The date of birth for the individual.",
        default=None,
    )
    address: Optional[List[Address]] = Field(
        description="Not used in this profile",
        default=None,
    )
    maritalStatus: Optional[CodeableConcept] = Field(
        description="Not used in this profile",
        default=None,
    )
    photo: Optional[List[Attachment]] = Field(
        description="Not used in this profile",
        default=None,
    )
    contact: Optional[List[PatientContact]] = Field(
        description="Not used in this profile",
        default=None,
    )
    communication: Optional[List[USCorePatientProfileCommunication]] = Field(
        description="Not used in this profile",
        default=None,
    )
    generalPractitioner: Optional[List[Reference]] = Field(
        description="Not used in this profile",
        default=None,
    )
    
    @property 
    def deceased(self):
        return get_type_choice_value_by_base(self, 
            base="deceased",
        )
        
    @field_validator(*('identifier',), mode="after", check_fields=None)
    @classmethod
    def identifier_slicing_cardinality_validator(cls, value):    
        return validate_slicing_cardinalities(cls, value, 
            field_name="identifier",
        )
        
    @model_validator(mode="after")
    def FHIR_ele_1_constraint_validator(self):
        return validate_element_constraint( 
            self,
            elements="generalPractitioner",
            expression='hasValue() or (children().count() > id.count())',
            human='All FHIR elements must have a @value or children',
            key='ele-1',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_pat_1_constraint_validator(self):
        return validate_element_constraint( 
            self,
            elements="contact",
            expression='name.exists() or telecom.exists() or address.exists() or organization.exists()',
            human="SHALL at least contain a contact's details or a reference to an organization",
            key='pat-1',
            severity='error',
        )
        
    @model_validator(mode="after")
    def deceased_type_choice_validator(self):
        return validate_type_choice_element( 
            self,
            field_types=['DateTime'],
            field_name_base="deceased",
            required=True,
        )
        
    @model_validator(mode="after")
    def FHIR_us_core_6_constraint_validator(self):
        return validate_element_constraint( 
            self,
            elements="name",
            expression="(family.exists() or given.exists()) xor extension.where(url='http://hl7.org/fhir/StructureDefinition/data-absent-reason').exists()",
            human='At least name.given and/or name.family are present or, if neither is available, the Data Absent Reason Extension is present.',
            key='us-core-6',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_1_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="identifier.where(type.coding.code = 'ACSN' and system = 'Onconova').exists()",
            human='The identifier element must contain the identifier for the Onconova logical pseudoidentifier slice.',
            key='o-pat-req-1',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_2_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="identifier.where(type.coding.code = 'MR').exists(system.exists() and value.exists())",
            human="The identifier element must contain the identifier for the clinical center's patient identifier slice.",
            key='o-pat-req-2',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_3_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='gender.exists()',
            human='The gender element is required and must be provided',
            key='o-pat-req-3',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_4_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression='birthDate.exists() and birthDate.hasValue()',
            human='The birthDate element is required and must be provided',
            key='o-pat-req-4',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_5_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="deceased.extension('http://hl7.org/fhir/StructureDefinition/onconova-ext-cancer-patient-vital-status').valueCodeableConcept.exists()",
            human='The vital status extension is required and must be provided.',
            key='o-pat-req-5',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_6_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="deceasedDateTime.extension('http://hl7.org/fhir/StructureDefinition/onconova-ext-cancer-patient-vital-status').valueCodeableConcept.coding.code = '419099009' implies (deceasedDateTime.hasValue() and deceasedDateTime.extension('http://hl7.org/fhir/StructureDefinition/onconova-ext-cancer-patient-cause-of-death').valueCodeableConcept.exists())",
            human='If the patient is deceased, the date of death and cause of death must be provided.',
            key='o-pat-req-6',
            severity='error',
        )
        
    @model_validator(mode="after")
    def FHIR_o_pat_req_7_constraint_validator(self):
        return validate_model_constraint( 
            self,
            expression="deceasedDateTime.extension('http://hl7.org/fhir/StructureDefinition/onconova-ext-cancer-patient-vital-status').valueCodeableConcept.coding.code = '261665006' implies extension('http://hl7.org/fhir/StructureDefinition/onconova-ext-cancer-patient-end-of-records').valueDate.hasValue()",
            human='If the patient vital status is unknown, the end of records must be provided.',
            key='o-pat-req-7',
            severity='error',
        )
        
